#include <iostream>
/*Include means import in python but some 
libraries in python are not to be imported called
standerd libraries but you have to import it in C++
bummer*/

int main()
{ 
   std::cout << "hello world" << std::endl;

   /*std = standerd library
   cout = output command similar to print function
   we have to inclde std before using standerd 
   libraries in C++
   endl = end line
   */
   
   return 0;
}
